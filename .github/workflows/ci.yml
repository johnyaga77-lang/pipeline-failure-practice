name: Failure Pipeline

on:
  push:
    branches:
      - ci-test

jobs:
  fail-job:
    runs-on: ubuntu-latest

   steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Failure Script
        run: bash fail.sh
